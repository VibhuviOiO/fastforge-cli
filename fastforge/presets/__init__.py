"""Built-in FastForge use-case presets, shipped inside the wheel.

These are the same files as `examples/use-cases/` in the repo, copied here so
PyPI users can run `fastforge new --preset <name>` without cloning the repo.
"""
